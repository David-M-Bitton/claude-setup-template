# Personal Context — read only what the task needs

This folder holds documents about you. **Don't load everything every session** — that wastes tokens. Use the decision tree below to open ONLY the file(s) a task actually needs.

Quick profile facts live in `~/Documents/Claude/Memory/personal.md` — read that first for a one-screen summary. Come into this folder only when a task needs the full detail.

## Decision-tree matrix

> Keep this table current. **Whenever you add, rename, or remove a file in this folder, update a row here in the same session** — list the file, when to read it, and skip it otherwise. This table is how future sessions decide what to open.

| If the task is about… | Read | Otherwise |
|---|---|---|
| A quick "who is this person / their role / preferences" answer | `~/Documents/Claude/Memory/personal.md` | — |
| _(add rows as you add files — e.g. "writing style / how I like things phrased")_ | `<file>` | skip |
| _(e.g. "current projects / what I'm working on")_ | `<file>` | skip |
| _(e.g. "bio / background / resume")_ | `<file>` | skip |
| _(e.g. "schedule / routines / recurring tasks")_ | `<file>` | skip |
| _(e.g. "tools / accounts / how I work")_ | `<file>` | skip |

If no row matches the task, don't open anything here — answer from `Memory/personal.md` or ask.
